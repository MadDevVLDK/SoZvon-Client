<Window x:Class="SoZvon.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero"
        xmlns:local="clr-namespace:SoZvon" mc:Ignorable="d"
        Icon="/images/app_ico/SoZvon_logo.ico" Title="SoZvon"
        Height="660" Width="1200"
        Cursor="Arrow" Closing="FormClosing"
        IsTabStop="False">

    <Window.Resources>
        <Storyboard x:Key="RoomPanelAnimation">
            <DoubleAnimation Storyboard.TargetName="MainGrid_RoomPanel" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" From="0" To="0" Duration="0:0:0.25" RepeatBehavior="1x" />
        </Storyboard>
        <Storyboard x:Key="RoomInfoPanelAnimation">
            <DoubleAnimation Storyboard.TargetName="MainGrid_RoomInfo" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" From="0" To="0" Duration="0:0:0.25" RepeatBehavior="1x" />
        </Storyboard>
        <Storyboard x:Key="CentreGridMarginAnimation">
            <ThicknessAnimation Storyboard.TargetName="Centre_Window_Grid" Storyboard.TargetProperty="Margin" From="0" To="0" Duration="0:0:0.25" RepeatBehavior="1x"/>
        </Storyboard>
        <Storyboard x:Key="OpacityAnimation">
            <DoubleAnimation  Storyboard.TargetName="Error_Grid" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:2" RepeatBehavior="1x"/>
        </Storyboard>



        <Style x:Key="VerticalThumb" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="4" />
                                <RowDefinition Height="4" />
                                <RowDefinition />
                                <RowDefinition Height="4" />
                                <RowDefinition Height="4" />
                            </Grid.RowDefinitions>
                            <Ellipse Grid.Row="0" Grid.RowSpan="2" Fill="#FFF4F4F5" />
                            <Rectangle Grid.Row="1" Grid.RowSpan="3" Fill="#FFF4F4F5" />
                            <Ellipse Grid.Row="3" Grid.RowSpan="2" Fill="#FFF4F4F5" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="HorizontalThumb" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="4" />
                                <ColumnDefinition Width="4" />
                                <ColumnDefinition />
                                <ColumnDefinition Width="4" />
                                <ColumnDefinition Width="4" />
                            </Grid.ColumnDefinitions>
                            <Ellipse Grid.Column="0" Grid.ColumnSpan="2" Fill="#FFF4F4F5" />
                            <Rectangle Grid.Column="1" Grid.ColumnSpan="3" Fill="#FFF4F4F5" />
                            <Ellipse Grid.Column="3" Grid.ColumnSpan="2" Fill="#FFF4F4F5" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <ControlTemplate x:Key="ScrollViewerControlTemplate" TargetType="{x:Type ScrollViewer}">
            <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Rectangle x:Name="Corner" Grid.Column="1" Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Grid.Row="1"/>
                <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanContentScroll="{TemplateBinding CanContentScroll}" CanHorizontallyScroll="False" CanVerticallyScroll="False" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" Grid.RowSpan="2" Margin="{TemplateBinding Padding}" Grid.ColumnSpan="2"/>
                <ScrollBar x:Name="PART_VerticalScrollBar" AutomationProperties.AutomationId="VerticalScrollBar" Cursor="Arrow" Grid.Column="1" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Grid.Row="0" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" ViewportSize="{TemplateBinding ViewportHeight}" Width="8"/>
                <ScrollBar x:Name="PART_HorizontalScrollBar" AutomationProperties.AutomationId="HorizontalScrollBar" Cursor="Arrow" Grid.Column="0" Maximum="{TemplateBinding ScrollableWidth}" Minimum="0" Orientation="Horizontal" Grid.Row="1" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" ViewportSize="{TemplateBinding ViewportWidth}" Height="8"/>
            </Grid>
        </ControlTemplate>
        <Style x:Key="ScrollBarPageButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Rectangle Fill="{TemplateBinding Background}" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>

            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="Width" Value="10"/>
            <Setter Property="MinWidth" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true" Background="#7FBFBFBF">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="0.00001*"/>
                            </Grid.RowDefinitions>
                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsMouseOver}" Grid.Row="1">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{StaticResource ScrollBarPageButton}"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{StaticResource ScrollBarPageButton}"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{DynamicResource VerticalThumb}" theme:ScrollChrome.ScrollGlyph="VerticalGripper" Width="8"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="Height" Value="10"/>
                    <Setter Property="MinHeight" Value="10"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid x:Name="Bg" SnapsToDevicePixels="true" Background="#7FA7A7A7">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.00001*"/>
                                    </Grid.ColumnDefinitions>
                                    <Track x:Name="PART_Track" Grid.Column="1" IsEnabled="{TemplateBinding IsMouseOver}">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Style="{StaticResource ScrollBarPageButton}"/>
                                        </Track.DecreaseRepeatButton>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Style="{StaticResource ScrollBarPageButton}"/>
                                        </Track.IncreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb theme:ScrollChrome.ScrollGlyph="HorizontalGripper" Style="{DynamicResource HorizontalThumb}" Height="8"/>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Name="MainGrid">

        <Grid Grid.ColumnSpan="1" Width="48" HorizontalAlignment="Left" Margin="0,0,0,29" >

            <Rectangle Fill="#ffc159" StrokeThickness="1" Stroke="#a28d6a"/>

            <Grid Name="SettingsOpen_Button" Cursor="Hand" MouseUp="AnyButton_UpMouse" MouseDown="AnyButton_DownMouse" MouseEnter="AnyButton_EnterLeaveMouse" MouseLeave="AnyButton_EnterLeaveMouse" Margin="0,11,0,0" Width="36" HorizontalAlignment="Center" Height="35" VerticalAlignment="Top">
                <Rectangle Tag="Background" Fill="White" RadiusX="10" RadiusY="10" Opacity="0.5" StrokeThickness="1.5" Stroke="#FF5A4B34"/>

                <Rectangle Fill="#FF947F5B" RadiusX="1.469" RadiusY="1.469" Margin="6,22,6,10"/>

                <Rectangle Fill="#FF947F5B" RadiusX="1.469" RadiusY="1.469" Margin="6,16,6,16"/>

                <Rectangle Fill="#FF947F5B" RadiusX="1.719" RadiusY="1.719" Margin="6,10,6,22"/>
            </Grid>

        </Grid>

        <Frame Name="LeftPanel" Background="#ffdb9e" Margin="47,0,0,29" HorizontalAlignment="Left" Width="294" IsTabStop="False"/>

        <Grid Name="MainGrid_RightPanel" Margin="340,0,22,29" Visibility="Visible">

            <Rectangle Fill="#ffc666" StrokeThickness="1" Stroke="#a28d6a" HorizontalAlignment="Left" Width="11"/>
            <Thumb Opacity="0" Background="Transparent" Cursor="SizeWE" BorderThickness="1" BorderBrush="#a28d6a" DragDelta="OnDrag_RightLeftPanelThumb" HorizontalAlignment="Left" Width="11"/>

            <Frame Name="RightPanel" Background="#ffdb9e" Margin="10,0,-1,0" IsTabStop="False"/>

        </Grid>

        <Rectangle Fill="#ffc666"  StrokeThickness="1" Stroke="#a28d6a" HorizontalAlignment="Right" Width="22" Margin="0,0,0,29"/>

        <Grid Name="Log_Grid" Margin="0,0,0,-1" Height="31" VerticalAlignment="Bottom">
            <Rectangle Tag="Background" Fill="#ffc666" StrokeThickness="1" Stroke="#a28d6a"/>
            <TextBlock Tag="Text" IsEnabled="False" Height="22" VerticalAlignment="Center" Margin="6,0,12,0" FontSize="18"/>
        </Grid>

        <Frame IsTabStop="False" Visibility="Hidden" Name="MainFrame" NavigationUIVisibility="Hidden" Background="#F8FFC666" Margin="0,0,0,30"/>

        <StackPanel Name="Error_StackPanel" Margin="20 32" Orientation="Vertical" VerticalAlignment="Bottom" HorizontalAlignment="Left"/>

    </Grid>



</Window>
    
    
    
    
    

